# Expert Visit Card 2026 — Современная визитная карточка эксперта

Производственный landing page для консультанта / эксперта на российском рынке.

## 🎯 Особенности

- ✨ **Тёмный минимализм** с футуристичным дизайном
- 🚀 **GSAP анимации** с плавными переходами
- 🎨 **Glassmorphism** для премиального вида
- 📱 **Mobile-first** адаптивный дизайн
- ⚡ **Лучшая производительность** с оптимизацией
- 🎬 **Плавный скролл** через Lenis
- 🎯 **Интерактивные элементы** с micro-animations
- 🔍 **SEO-оптимизирован** с правильными meta-тегами

## 📁 Структура проекта

```
expert-visit-card/
├── index.html              # Основной HTML файл со всеми 9 секциями
├── package.json            # Зависимости проекта
├── vite.config.js          # Конфиг Vite
├── postcss.config.js       # PostCSS конфиг для Tailwind
├── tailwind.config.js      # Tailwind конфиг с кастомизацией
├── src/
│   ├── main.js            # GSAP анимации и интерактивность
│   └── styles.css         # Кастомные стили, градиенты, анимации
└── README.md
```

## 🚀 Быстрый старт

### Установка зависимостей

```bash
cd expert-visit-card
npm install
```

### Разработка

```bash
npm run dev
```

Откроется на `http://localhost:5173`

### Продакшен

```bash
npm run build
```

Готовые файлы в папке `dist/`

## 🎨 Используемые технологии

### Frontend Stack
- **Vite** — молниеносный сборщик
- **HTML5** — семантическая разметка
- **TailwindCSS 3** — утилитарный CSS фреймворк
- **Vanilla JavaScript** — без зависимостей
- **GSAP 3** — профессиональные анимации
- **Lenis** — плавный скролл
- **Lucide Icons** — современные SVG иконки

### Производительность
- Критический CSS встроен
- Lazy loading для изображений
- Code splitting для GSAP
- Минификация и оптимизация бандла

## 📋 Разделы страницы

### 1. Hero Section
- Анимированный фон с градиентами
- Плавающие blob элементы
- CTA кнопки с gradient эффектом
- Параллакс эффект

**Анимации:**
- Fade-in элементов при загрузке
- Floating blobs в фоне
- Parallax на прокрутке

### 2. Value Proposition Cards
- 3 карточки с основными проблемами
- Glass-морфизм дизайн
- Hover effects с поднятием карточки
- Иконки с результатами

**Анимации:**
- Stagger reveal на скролле
- Lift effect на hover
- Glow shadow при наведении

### 3. Services Grid
- 6 карточек услуг в адаптивной сетке
- Иконки с hover-анимацией
- Ценовые теги
- Interactive border effect

**Анимации:**
- Icon rotation на hover
- Card lift с задержкой
- Glow box-shadow

### 4. Portfolio Cases Slider
- Слайдер с кейсами
- Управление стрелками
- Метрики результатов
- Glass-дизайн карточек

**Анимации:**
- Smooth slide transition
- Scale на hover каждого слайда
- Button press effect

### 5. Process Timeline
- 5 этапов процесса
- Vertical timeline дизайн
- Animated dots с цветовым градиентом
- Stagger animation

**Анимации:**
- Dot появляются с scale
- Content slideIn с X-смещением
- Glow dot на hover

### 6. Interactive Price Calculator
- 3 ползунка для расчёта
- Реал-тайм обновление цены
- Glass-морфизм контейнер
- Smooth number animation

**Функциональность:**
- GSAP animated counter
- Smooth transitions
- Real-time calculation

### 7. Testimonials Slider
- Слайдер с отзывами
- Звёздные рейтинги
- Avatar placeholders
- Navigation controls

**Анимации:**
- Smooth horizontal scroll
- Fade transitions
- Button hover effects

### 8. Call-to-Action Block
- Центральный блок с призывом
- Gradient фон
- Двойные CTA кнопки
- Подтверждение времени ответа

### 9. Contacts Section
- 3 способа связи (Telegram, Email, Phone)
- Кликабельные ссылки
- Footer с информацией
- Sticky Telegram кнопка

## 🎬 Ключевые анимации

### Reveal on Scroll
```javascript
// Элементы появляются при попадании в viewport
gsap.from(element, {
  scrollTrigger: {
    trigger: element,
    start: 'top 80%',
  },
  opacity: 0,
  y: 50,
  duration: 1,
  ease: 'power3.out',
});
```

### Hover Effects
```javascript
// Поднятие карточки с glow при наведении
gsap.to(card, {
  y: -10,
  boxShadow: '0 20px 40px rgba(59, 130, 246, 0.2)',
  duration: 0.3,
});
```

### Counter Animation
```javascript
// Плавное счётывание чисел
gsap.fromTo(
  { frame: 0 },
  { frame: 1, onUpdate: updateCounter },
  { duration: 2, ease: 'power1.inOut' }
);
```

### Smooth Scroll (Lenis)
```javascript
const lenis = new Lenis({
  duration: 1.2,
  easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
  smooth: true,
});
```

## 🎨 Glassmorphism реализация

### Glass класс
```css
.glass {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 16px;
}
```

### Glass-dark вариант
```css
.glass-dark {
  background: rgba(15, 23, 42, 0.4);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.05);
}
```

## 🎨 Цветовая палитра

```javascript
colors: {
  'dark': {
    50: '#f9fafb',
    900: '#0a0e27',
    950: '#050814',
  },
  'accent': {
    400: '#60a5fa',  // Blue
    500: '#3b82f6',
    600: '#2563eb',
  },
  'neon': {
    cyan: '#00d9ff',
    purple: '#d946ef',
    lime: '#bef264',
  },
}
```

## ⚡ Оптимизация производительности

### 1. Hardware Acceleration
```css
.transform-gpu {
  will-change: transform;
  transform: translateZ(0);
}
```

### 2. Lazy Loading
- Изображения загружаются при необходимости
- Defer non-critical scripts

### 3. GSAP Optimization
```javascript
// Использование ScrollTrigger.refresh() после изменений
ScrollTrigger.refresh();

// Lagsmoothing для плавной анимации
gsap.ticker.lagSmoothing(0);
```

### 4. CSS Containment
```css
.service-item {
  contain: layout style paint;
}
```

### 5. Reduce Motion для accessibility
```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

## 📱 Responsive Design

- **Desktop** (1024px+): Полный layout с параллаксом
- **Tablet** (768px-1023px): Адаптированный grid
- **Mobile** (< 768px): Single column, большие touch areas

```css
@media (max-width: 768px) {
  h1 { font-size: 2rem; }
  h2 { font-size: 1.5rem; }
  .container-max { padding: 0 16px; }
}
```

## 🔧 Кастомизация

### Изменить цвета
Отредактируйте `tailwind.config.js`:
```javascript
colors: {
  'primary': '#your-color',
  'accent': '#your-accent',
}
```

### Добавить новую секцию
1. Добавьте HTML в `index.html`
2. Добавьте анимацию в `src/main.js`
3. Используйте классы `.reveal`, `.glass-dark` для консистентности

### Изменить контент
Все текст находится в `index.html`. Просто заменяйте:
- Названия услуг
- Описания
- Ценовые точки
- Ссылки в контактах

## 📊 SEO Оптимизация

- ✅ Правильные meta-теги
- ✅ Структурированные заголовки (h1 → h3)
- ✅ Alt текст для иконок
- ✅ Fast Core Web Vitals
- ✅ Mobile-friendly design
- ✅ Structured data ready

## 🚀 Deploy

### Vercel (рекомендуется)
```bash
npm install -g vercel
vercel
```

### GitHub Pages
```bash
npm run build
# Push dist/ на gh-pages branch
```

### Netlify
Свяжите репозиторий и настройте:
- Build command: `npm run build`
- Publish directory: `dist`

## 📝 Лицензия

MIT — свободно используйте в своих проектах

## 💬 Поддержка

Нужна помощь? Вот что вы можете сделать:
- Читайте комментарии в коде
- Смотрите примеры GSAP в `main.js`
- Проверяйте классы Tailwind в `tailwind.config.js`

---

**Создано в 2026 году. Для профессионалов, которые хотят выглядеть профессионально.**
